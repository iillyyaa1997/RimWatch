# 🖱️ Scrolling Fix for Settings Window

**Дата исправления:** 2025-11-07  
**Версия:** 0.7.3+

---

## 🎯 Проблема

В окне настроек мода (`Esc → Options → Mod Settings → RimWatch`) не помещались все настройки на экран. Нижние секции (Debug Mode Settings, кнопки действий) были недоступны.

**Симптомы:**
- ❌ Не видны настройки Debug Mode
- ❌ Не видна кнопка "Apply Settings to Autopilot"
- ❌ Не видна кнопка "Reset to Defaults"
- ❌ Не видна кнопка "📂 Open Logs Folder"

---

## ✅ Решение

Добавлен **скроллинг** в окно настроек с использованием `Widgets.BeginScrollView()`.

### Изменения в коде:

#### 1. Добавлена переменная для позиции скролла

```csharp
public class RimWatchMod : Mod
{
    // ...existing code...
    
    // Scroll position for settings window
    private static Vector2 scrollPosition = Vector2.zero;
    
    // ...rest of code...
}
```

#### 2. Обернут весь контент в ScrollView

**До:**
```csharp
public override void DoSettingsWindowContents(Rect inRect)
{
    Listing_Standard listing = new Listing_Standard();
    listing.Begin(inRect);
    
    // ... settings ...
    
    listing.End();
}
```

**После:**
```csharp
public override void DoSettingsWindowContents(Rect inRect)
{
    // Calculate scrollable area height (approximate)
    float contentHeight = 1200f; // Enough for all settings
    
    // Create scrollable view
    Rect viewRect = new Rect(0f, 0f, inRect.width - 20f, contentHeight);
    Rect outRect = new Rect(inRect.x, inRect.y, inRect.width, inRect.height);
    
    Widgets.BeginScrollView(outRect, ref scrollPosition, viewRect);
    
    Listing_Standard listing = new Listing_Standard();
    listing.Begin(viewRect);
    
    // ... settings ...
    
    listing.End();
    Widgets.EndScrollView();
}
```

---

## 📊 Что изменилось?

### До исправления:
- ❌ Высота контента ограничена размером окна
- ❌ Нижние настройки не видны
- ❌ Нет возможности прокрутить вниз

### После исправления:
- ✅ Вся высота контента: **1200 пикселей**
- ✅ Появился **scrollbar** справа
- ✅ Можно прокрутить до любой настройки
- ✅ Все секции доступны

---

## 🔧 Технические детали

### Параметры скроллинга:

```csharp
float contentHeight = 1200f;  // Общая высота контента
Rect viewRect = new Rect(0f, 0f, inRect.width - 20f, contentHeight);  // -20px для scrollbar
Rect outRect = new Rect(inRect.x, inRect.y, inRect.width, inRect.height);  // Видимая область
```

### Секции в окне настроек (сверху вниз):

1. **Заголовок** - "RimWatch - AI Autopilot Settings"
2. **Automation Categories** - 8 чекбоксов (Building, Work, Farming, Defense, Trade, Medical, Social, Research)
3. **AI Storyteller** - выбор стиля автопилота
4. **Work Priority Settings** - настройка приоритетов работы
5. **Advanced Settings** - дополнительные опции
6. **Debug Mode Settings** - настройки отладки (теперь видны!)
7. **Кнопки действий** - Apply Settings, Reset to Defaults (теперь видны!)
8. **Tip** - подсказка о Shift+R

**Общая высота:** ~1150-1200 пикселей

---

## 🧪 Как проверить?

### Тест 1: Скролл работает
1. Открой настройки: `Esc → Options → Mod Settings → RimWatch`
2. **Ожидается:** Справа появился scrollbar
3. Прокрути вниз колесиком мыши или scrollbar'ом
4. **Ожидается:** Видны секции Debug Mode Settings и кнопки

### Тест 2: Все секции доступны
1. Прокрути до самого низа
2. **Ожидается:** Видна секция "🐛 Debug Mode Settings"
3. **Ожидается:** Видны кнопки "Apply Settings to Autopilot" и "Reset to Defaults"
4. **Ожидается:** Видна кнопка "📂 Open Logs Folder" (если File Logging включен)

### Тест 3: ScrollPosition сохраняется
1. Прокрути вниз
2. Закрой окно настроек (OK/Cancel)
3. Открой снова
4. **Ожидается:** Позиция скролла **не сохраняется** (это нормально, начинается с верха)

---

## 💡 Дополнительная информация

### Почему contentHeight = 1200f?

Это **приблизительный** расчет:
- Каждый checkbox ≈ 30px
- Каждый Label ≈ 25px
- Каждый Gap ≈ 6-12px
- Каждая Button ≈ 35px

**Итого:**
- 8 checkboxes (Automation Categories) = 240px
- 1 dropdown (AI Storyteller) = 50px
- 1 checkbox (Work Priority) = 30px
- 3 checkboxes + 1 slider (Advanced) = 120px
- 2 checkboxes + button (Debug Mode) = 100px
- 2 buttons (Actions) = 70px
- Labels + Gaps ≈ 590px

**Сумма:** ~1200px (с запасом)

### Почему viewRect.width = inRect.width - 20f?

**-20 пикселей** - это место для **scrollbar**. Без этого отступа контент будет заезжать под scrollbar.

### Можно ли сделать динамический contentHeight?

Да, можно рассчитывать `listing.CurHeight` после всех элементов, но для простоты используется константа. Если добавятся новые настройки, просто увеличь `contentHeight`.

---

## 🔄 Версии

### v0.7.3+ (2025-11-07)
- ✅ Добавлен скроллинг в окно настроек
- ✅ Все настройки теперь доступны
- ✅ ScrollView с высотой 1200px

---

## 📝 Примечания

1. **Scrollbar появляется автоматически** когда `contentHeight > inRect.height`
2. **Позиция скролла НЕ сохраняется** между открытиями окна (начинается сверху)
3. **Ширина scrollbar ≈ 16px**, но используется -20px для безопасности

---

**Статус:** ✅ Скомпилировано и развернуто (2025-11-07)  
**Версия:** 0.7.3+  
**Файл:** `Source/RimWatch/RimWatchMod.cs`


