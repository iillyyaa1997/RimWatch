# 🐛 Debug Mode & File Logging Feature

**Дата добавления:** 2025-11-07  
**Версия:** 0.7.3

---

## 🎯 Что добавлено?

### 1. 🔍 Debug Mode

**Новая глобальная настройка** для показа DEBUG-уровня логов в консоли игры.

**Возможности:**
- ✅ Показывает **все** DEBUG логи в консоли RimWorld
- ✅ Работает независимо от Dev Mode
- ✅ Можно включить/выключить в любой момент
- ✅ Очень подробные логи для диагностики проблем

**Где включить:**
```
Options → Mod Settings → RimWatch → 🐛 Debug Mode Settings → 🔍 Enable Debug Mode
```

**Примеры DEBUG логов:**
```
[RimWatch] [DEBUG] DefenseAutomation: Found 3 unarmed colonists, searching for weapons...
[RimWatch] [DEBUG] DefenseAutomation: Found 5 available weapons: пистолет, дробовик, автомат...
[RimWatch] [DEBUG] BuildingAutomation: Checking for bed placement...
[RimWatch] [DEBUG] FarmingAutomation: Analyzing food needs...
```

---

### 2. 📝 File Logging

**Новая система записи всех логов в файл** для удобной диагностики и отправки bug reports.

**Возможности:**
- ✅ Записывает **все** логи (INFO, WARNING, ERROR, DEBUG) в файл
- ✅ Автоматически создает файл с timestamp при первом логе
- ✅ Файлы хранятся в отдельной папке `RimWatch_Logs`
- ✅ Каждый запуск игры = новый файл (не перезаписывается)
- ✅ Форматированный вывод с timestamp и уровнем

**Где включить:**
```
Options → Mod Settings → RimWatch → 🐛 Debug Mode Settings → 📝 Enable File Logging
```

**Кнопка "📂 Open Logs Folder"** открывает папку с логами в проводнике.

**Пример содержимого файла:**
```
═══════════════════════════════════════════════════════════
RimWatch Log File
Started: 2025-11-07 15:30:45
RimWorld Version: 1.6
═══════════════════════════════════════════════════════════

[15:30:45.123] [INFO] Initializing RimWatch v0.1.0-dev
[15:30:45.456] [INFO] Initial settings applied to Core
[15:30:45.789] [DEBUG] DefenseAutomation: Found 3 unarmed colonists, searching for weapons...
[15:30:46.012] [INFO] 🗡️ DefenseAutomation: Equipped 3 colonists with weapons
[15:30:50.234] [WARNING] BuildingAutomation: Could not find suitable location for kitchen
[15:31:00.567] [ERROR] FarmingAutomation: Failed to create growing zone
```

---

## 📁 Расположение файлов логов

### macOS:
```
~/Library/Application Support/RimWorld/RimWatch_Logs/
```

### Windows:
```
C:\Users\<Username>\AppData\LocalLow\Ludeon Studios\RimWorld by Ludeon Studios\RimWatch_Logs\
```

### Linux:
```
~/.config/unity3d/Ludeon Studios/RimWorld by Ludeon Studios/RimWatch_Logs/
```

**Формат имени файла:**
```
RimWatch_2025-11-07_15-30-45.log
```

---

## 🔧 Технические детали

### Изменения в коде:

#### 1. `RimWatchLogger.cs`

**Добавлены:**
- `FileLoggingEnabled` - флаг для включения записи в файл
- `DebugModeEnabled` - флаг для показа DEBUG логов
- `InitializeFileLogging()` - инициализация файла лога
- `WriteToFile()` - запись в файл с thread-safety
- `FormatLogMessage()` - форматирование логов с timestamp
- `GetLogFilePath()` - получение пути к текущему файлу
- `OpenLogsDirectory()` - открытие папки с логами

**Модифицированы все методы:**
- `Info()` → пишет в файл если `FileLoggingEnabled`
- `Warning()` → пишет в файл если `FileLoggingEnabled`
- `Error()` → пишет в файл если `FileLoggingEnabled`
- `Debug()` → показывает если `DebugModeEnabled` ИЛИ `Prefs.DevMode`, всегда пишет в файл

#### 2. `RimWatchSettings.cs`

**Добавлены поля:**
```csharp
public bool debugModeEnabled = false;
public bool fileLoggingEnabled = false;
```

**Обновлен `ApplyToCore()`:**
```csharp
// Apply debug mode settings to logger
Utils.RimWatchLogger.DebugModeEnabled = debugModeEnabled;
Utils.RimWatchLogger.FileLoggingEnabled = fileLoggingEnabled;
```

#### 3. `RimWatchMod.cs`

**Добавлена секция UI:**
```csharp
// === Debug Mode Settings ===
listing.CheckboxLabeled("🔍 Enable Debug Mode", ref Settings.debugModeEnabled);
listing.CheckboxLabeled("📝 Enable File Logging", ref Settings.fileLoggingEnabled);

if (Settings.fileLoggingEnabled)
{
    listing.Label($"Log file: {Path.GetFileName(logPath)}");
    if (listing.ButtonText("📂 Open Logs Folder"))
    {
        RimWatchLogger.OpenLogsDirectory();
    }
}
```

---

## 🧪 Как использовать?

### Сценарий 1: Включить DEBUG логи в консоли

1. Открой настройки мода: `Esc → Options → Mod Settings → RimWatch`
2. Прокрути до **🐛 Debug Mode Settings**
3. Включи **🔍 Enable Debug Mode**
4. Нажми **Apply Settings to Autopilot**
5. В консоли игры (F12 / ~) появятся детальные [DEBUG] логи

**Плюсы:** Видишь что делает автопилот в реальном времени  
**Минусы:** Очень много логов, может тормозить консоль

---

### Сценарий 2: Записывать логи в файл для отправки разработчику

1. Открой настройки мода: `Esc → Options → Mod Settings → RimWatch`
2. Прокрути до **🐛 Debug Mode Settings**
3. Включи **📝 Enable File Logging**
4. Нажми **Apply Settings to Autopilot**
5. Играй, воспроизводи проблему
6. Нажми **📂 Open Logs Folder**
7. Найди файл `RimWatch_<timestamp>.log`
8. Отправь файл разработчику

**Плюсы:** Вся информация сохранена, можно отправить разработчику  
**Минусы:** Занимает место на диске (файлы могут быть большими)

---

### Сценарий 3: Максимальная диагностика (Debug + File)

1. Включи **оба** флага: Debug Mode + File Logging
2. Играй с проблемой
3. DEBUG логи видны в консоли (F12)
4. Все логи (включая DEBUG) записываются в файл

**Плюсы:** Максимум информации  
**Минусы:** Много логов в консоли + большие файлы

---

## 📊 Сравнение режимов логирования

| Режим | Консоль | Файл | Когда использовать |
|-------|---------|------|-------------------|
| **По умолчанию** | INFO, WARNING, ERROR | Ничего | Обычная игра |
| **Dev Mode** | INFO, WARNING, ERROR, DEBUG | Ничего | Разработка, быстрая диагностика |
| **Debug Mode** | INFO, WARNING, ERROR, DEBUG | Ничего | Детальная диагностика в реальном времени |
| **File Logging** | INFO, WARNING, ERROR | Всё (включая DEBUG) | Сохранение логов для bug report |
| **Debug + File** | INFO, WARNING, ERROR, DEBUG | Всё (включая DEBUG) | Максимальная диагностика |

---

## ⚠️ Предупреждения

### 1. Размер файлов логов

File Logging может создавать **большие файлы** при длительной игре:
- **1 час игры** ≈ 5-20 MB (зависит от активности автопилота)
- **10 часов игры** ≈ 50-200 MB

**Рекомендация:** Включай File Logging только когда нужно диагностировать проблему, затем выключай.

### 2. Производительность

Debug Mode генерирует **много** логов:
- В среднем **100-500** строк в минуту при активном автопилоте
- Может замедлить консоль игры (F12) при прокрутке

**Рекомендация:** Не включай Debug Mode без необходимости.

### 3. Приватность

Файлы логов могут содержать:
- Имена колонистов
- Названия сохранений
- Время игры

**Рекомендация:** Проверь файл перед отправкой, если не хочешь делиться этой информацией.

---

## 🔍 Формат логов в файле

```
[HH:mm:ss.fff] [LEVEL] Message
```

**Примеры:**
```
[15:30:45.123] [INFO] Initializing RimWatch v0.1.0-dev
[15:30:45.456] [DEBUG] DefenseAutomation: Found 3 unarmed colonists
[15:30:46.789] [WARNING] BuildingAutomation: Could not find suitable location
[15:31:00.012] [ERROR] FarmingAutomation: Failed to create growing zone
```

**Разбор:**
- `[15:30:45.123]` - timestamp с миллисекундами
- `[INFO]` - уровень лога (DEBUG / INFO / WARNING / ERROR)
- `Message` - текст сообщения

---

## 💡 Советы по использованию

### 1. Для пользователей (нашел баг):
1. Включи **File Logging**
2. Воспроизведи проблему
3. Отправь файл лога разработчику вместе с описанием

### 2. Для тестеров:
1. Включи **Debug Mode** для наблюдения за автопилотом
2. Если нужно сохранить логи - включи **File Logging**
3. Регулярно проверяй папку `RimWatch_Logs` и удаляй старые файлы

### 3. Для разработчиков:
1. Используй **Debug Mode** для live debugging
2. Используй **File Logging** для анализа после воспроизведения проблемы
3. Добавляй новые DEBUG логи в критические места кода

---

## 📝 Примеры полезных DEBUG логов

### DefenseAutomation:
```
[DEBUG] DefenseAutomation: Found 3 unarmed colonists, searching for weapons...
[DEBUG] DefenseAutomation: Found 5 available weapons: пистолет, дробовик, автомат, снайперка, лук
[DEBUG] DefenseAutomation: No weapons available to equip (total: 8, forbidden: 8)
```

### BuildingAutomation:
```
[DEBUG] BuildingAutomation: Checking for bed placement at radius 5
[DEBUG] BuildingAutomation: Found suitable location at (127, 89)
[DEBUG] BuildingAutomation: Placed blueprint for bed
```

### FarmingAutomation:
```
[DEBUG] FarmingAutomation: Analyzing food needs - current: 100, threshold: 500
[DEBUG] FarmingAutomation: Need to create growing zones
[DEBUG] FarmingAutomation: Created growing zone at (50, 60) - 121 cells
```

---

## 🚀 Что дальше?

### Планы на будущее:
- [ ] Добавить фильтры логов по категориям (только Defense, только Building, и т.д.)
- [ ] Добавить автоматическую очистку старых логов (>7 дней)
- [ ] Добавить экспорт логов в формате JSON для анализа
- [ ] Добавить in-game viewer логов (без открытия файлов)

---

## 📖 Дополнительные материалы

- **[README](README.md)**
- **[BUGFIX v0.7.2](BUGFIX_V072.md)**
- **[TESTING GUIDE](TESTING_GUIDE.md)**

---

**Статус:** ✅ Скомпилировано и развернуто (2025-11-07)  
**Версия:** 0.7.3  
**Совместимость:** RimWorld 1.6.4630+

