# 🐛 Отладка UI Кнопки RimWatch

## ✅ Что Сделано

### 1. **Исправлена позиция кнопки**
```csharp
// БЫЛО (кнопка за экраном):
private const float MarginRight = 180f;
private const float MarginTop = 0f;

// СТАЛО (правый верхний угол, видимо):
private const float ButtonSize = 40f;
private const float MarginRight = 10f;
private const float MarginTop = 7f;

// Правильный расчет X координаты:
UnityEngine.Screen.width - MarginRight - ButtonSize
```

**Почему было неправильно:**
- `MarginRight = 180f` отступало слишком далеко влево
- X координата рассчитывалась как `Screen.width - 180`, но без вычета размера кнопки
- Кнопка частично выходила за границы экрана

### 2. **Заменен эмодзи на ASCII текст**
```csharp
// БЫЛО (эмодзи не отображался на macOS):
Widgets.ButtonText(rect, "🎭")

// СТАЛО (простой текст):
Text.Font = GameFont.Small;
Widgets.ButtonText(rect, "RW")
```

**Почему:**
- Эмодзи 🎭 может не отображаться в Unity IMGUI на некоторых системах
- RimWorld использует специальные шрифты, не всегда поддерживающие эмодзи
- "RW" = RimWatch - понятная аббревиатура

### 3. **Добавлен отладочный лог**
```csharp
private static bool firstDrawLogged = false;

public static void Draw()
{
    if (!firstDrawLogged)
    {
        RimWatchLogger.Debug($"RimWatch button drawing at: x={rect.x}, y={rect.y}, w={rect.width}, h={rect.height}, Screen={UnityEngine.Screen.width}x{UnityEngine.Screen.height}");
        firstDrawLogged = true;
    }
    // ...
}
```

**Что покажет:** Точные координаты кнопки при первой отрисовке

---

## 🔍 Как Проверить Кнопку

### 1. **Запусти игру**
- Загрузи сохранение или начни новую игру
- **ВАЖНО:** Кнопка появится **только в игре**, НЕ в главном меню!

### 2. **Где искать кнопку?**
- **Правый верхний угол экрана**
- Рядом с кнопкой Dev Mode (если включен)
- Цвет кнопки зависит от статуса:
  - 🔴 **Красная** - автопилот выключен (по умолчанию)
  - 🟢 **Зеленая** - автопилот работает
  - 🟡 **Желтая** - предупреждение
  - ⚪ **Серая** - неактивно

### 3. **Посмотри логи**
После запуска игры ищи в `Player.log`:

```
[RimWatch] UI integration initialized
[RimWatch] [DEBUG] RimWatch button drawing at: x=1234, y=7, w=40, h=40, Screen=1920x1080
```

**Где найти `Player.log`:**
```bash
# macOS:
~/Library/Logs/Unity/Player.log

# Или открой через терминал:
tail -f ~/Library/Logs/Unity/Player.log | grep RimWatch
```

---

## 🎯 Возможные Проблемы

### Проблема 1: "Кнопка не видна, но лог есть"

**Причина:** Кнопка перекрыта другими модами

**Решение:** Измени позицию в `RimWatchButton.cs`:

```csharp
// Попробуй разные значения:
private const float MarginTop = 50f;  // Опустить вниз
private const float MarginRight = 60f; // Сдвинуть влево
```

### Проблема 2: "Лога 'UI integration initialized' нет"

**Причина:** Патч не применился или игра не запущена

**Решение:**
1. Убедись, что **игра запущена** (не главное меню!)
2. Проверь, что Harmony работает:
   ```
   [RimWatch] Harmony patches applied
   ```
3. Перезагрузи игру полностью

### Проблема 3: "Кнопка есть, но не кликабельна"

**Причина:** Другой мод перехватывает клики

**Решение:** Попробуй изменить `z-index` (порядок отрисовки):

```csharp
// В UI_Patch.cs, добавь приоритет:
[HarmonyPatch(typeof(UIRoot_Play), "UIRootOnGUI")]
[HarmonyPriority(Priority.Last)]  // Рисовать последним = поверх всех
public static class UI_Integration_Patch
```

### Проблема 4: "Кнопка мигает или исчезает"

**Причина:** Конфликт с другими модами (например, Camera+)

**Решение:** Проверь порядок загрузки модов:
1. RimWatch должен быть **после** Harmony
2. RimWatch должен быть **после** UI модов (RimHUD, Camera+, etc.)

---

## 🧪 Тестовый Патч (Для Экстренной Отладки)

Если кнопка вообще не появляется, добавь временный патч:

```csharp
// Временный файл: Source/RimWatch/Patches/DebugUI_Patch.cs
using HarmonyLib;
using UnityEngine;
using Verse;
using RimWatch.Utils;

namespace RimWatch.Patches
{
    [HarmonyPatch(typeof(UIRoot_Play), "UIRootOnGUI")]
    public static class DebugUI_Patch
    {
        [HarmonyPostfix]
        public static void Postfix()
        {
            // Рисуем ОГРОМНУЮ красную кнопку для теста
            Rect testRect = new Rect(100, 100, 200, 100);
            
            Color old = GUI.color;
            GUI.color = Color.red;
            
            if (Widgets.ButtonText(testRect, "TEST BUTTON - RimWatch"))
            {
                RimWatchLogger.Info("TEST BUTTON CLICKED!");
            }
            
            GUI.color = old;
        }
    }
}
```

**Если эта кнопка появится** → проблема в позиции/размере основной кнопки
**Если не появится** → проблема в патче Harmony или другой мод блокирует UI

---

## 📊 Сравнение с Рабочими Модами

### HugsLib Mod Options Button

**Их подход:**
```csharp
[HarmonyPatch(typeof(UIRoot_Play))]
[HarmonyPatch("UIRootOnGUI")]
public static class UIRoot_Play_UIRootOnGUI_Patch
{
    [HarmonyPostfix]
    public static void DrawModButton()
    {
        const float size = 32f;
        Rect buttonRect = new Rect(UI.screenWidth - size - 180f, 4f, size, size);
        if (Widgets.ButtonImage(buttonRect, HugsLibContent.Icon))
        {
            // ...
        }
    }
}
```

**Ключевые отличия:**
1. Они используют `ButtonImage` вместо `ButtonText`
2. Их позиция: `x = Screen.width - 32 - 180` (180px от правого края)
3. Они используют текстуру вместо текста

**Наш подход (RimWatch):**
- Позиция: `x = Screen.width - 10 - 40` (10px от правого края) ✅
- Размер: 40x40px (чуть больше, чем у HugsLib) ✅
- Текст "RW" вместо иконки (временно) ✅

---

## 🎨 Следующие Шаги (После Появления Кнопки)

### 1. **Создать иконку**
```
RimWatch/Textures/UI/RimWatchIcon.png
Размер: 128x128px
Формат: PNG с прозрачностью
```

### 2. **Заменить текст на иконку**
```csharp
// Загрузить текстуру (один раз при инициализации)
private static readonly Texture2D ButtonIcon = 
    ContentFinder<Texture2D>.Get("UI/RimWatchIcon");

// Использовать в Draw()
if (Widgets.ButtonImage(rect, ButtonIcon))
{
    OnLeftClick();
}
```

### 3. **Добавить анимацию**
```csharp
// Вращение иконки когда автопилот активен
if (RimWatchCore.AutopilotEnabled)
{
    float rotation = (Time.realtimeSinceStartup * 30f) % 360f;
    Matrix4x4 matrix = GUI.matrix;
    GUIUtility.RotateAroundPivot(rotation, rect.center);
    // ... draw button ...
    GUI.matrix = matrix;
}
```

---

## ✅ Чеклист Отладки

- [x] Мод компилируется без ошибок
- [x] Мод установлен в правильный путь
- [x] Harmony патч применяется
- [x] Лог инициализации есть
- [ ] **Игра ЗАПУЩЕНА** (не главное меню!)
- [ ] Лог отрисовки кнопки есть
- [ ] Кнопка видна на экране
- [ ] Кнопка кликабельна (ЛКМ)
- [ ] Правый клик работает
- [ ] Tooltip появляется

---

## 📝 Итоговые Изменения

### Файл: `RimWatchButton.cs`

**Изменения:**
1. Размер кнопки: `32f` → `40f`
2. Отступ справа: `180f` → `10f`
3. Отступ сверху: `0f` → `7f`
4. Расчет X: добавлено вычитание размера кнопки
5. Текст кнопки: `"🎭"` → `"RW"`
6. Добавлен отладочный лог координат

**Почему:** 
- Кнопка была за пределами видимой области
- Эмодзи не отображался
- Отсутствовала отладочная информация

---

## 🚀 Финальная Проверка

**Запусти игру прямо сейчас и:**

1. Начни новую игру или загрузи сохранение
2. Посмотри в **правый верхний угол** экрана
3. Ищи **красную кнопку "RW"** (40x40 пикселей)
4. Проверь лог: `tail -f ~/Library/Logs/Unity/Player.log | grep RimWatch`

**Ожидаемый результат:**
```
[RimWatch] UI integration initialized
[RimWatch] [DEBUG] RimWatch button drawing at: x=1870, y=7, w=40, h=40, Screen=1920x1080
```

**Если кнопки нет:**
- Сделай скриншот правого верхнего угла
- Скопируй полный лог с `[RimWatch]` сообщениями
- Проверь список активных модов (может быть конфликт)

---

**Дата:** 7 ноября 2025  
**Статус:** ✅ Исправления применены и задеплоены  
**Следующий шаг:** Тестирование в игре

