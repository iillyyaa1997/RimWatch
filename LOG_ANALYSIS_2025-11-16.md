## 🧠 RimWatch – Анализ логов (2025‑11‑16)

Этот файл фиксирует анализ последней игровой сессии, в которой колония полностью погибла. Основан на:

- `RimWatch_2025-11-16_01-53-12.log`
- `decisions_2025-11-16.json`

---

### 1. Общая картина по логам

- Объём логов за сессию:
  - **INFO** ≈ 149,652 сообщений
  - **DEBUG** ≈ 73,351 сообщений
  - **WARNING** ≈ 30,314 сообщений
  - **ERROR** ≈ 6,810 сообщений
- Топ‑источники логов:
  - `DefenseAutomation` ~ 22k сообщений
  - `WorkAutomation` ~ 5.3k
  - `MedicalAutomation` ~ 2.2k
  - `FarmingAutomation` ~ 1.5k
  - `BuildingAutomation` ~ 739

Разбор ошибок:

- **6724** ошибок от `ColonistCommandSystem`
- **86** ошибок от `ConstructionDiagnostics`

Разбор предупреждений (top):

- ~7.3k от `WorkAutomation`
- ~4.0k от `ColonistCommandSystem`
- ~1.9k от `MedicalAutomation`
- ~1.0k от `ResourceAutomation`
- ~0.6k от `BuildingAutomation` / `LocationFinder`

Вывод: колонию «сломали» не только игровые события, но и логика автоматики (особенно `ColonistCommandSystem`) плюс отсутствие троттлинга логов.

---

### 2. Критический баг №1: Rescue в ColonistCommandSystem

Фрагменты из лога:

- Многократно:
  - `ColonistCommandSystem: Queued Rescue task (priority 100)`
  - `ColonistCommandSystem: Error executing Rescue: Object reference not set to an instance of an object`
- Это идёт пачками десятки раз подряд.

Что происходит сейчас:

- `DetectAndQueueEmergencies` обнаруживает **downed** колониста и постоянно добавляет `Rescue`‑задачи.
- `ProcessTaskQueue` вытаскивает задачи и зовёт `ExecuteEmergencyTask → ExecuteRescue`.
- Внутри `ExecuteRescue` возникает `NullReferenceException`.
- В catch логируется только `ex.Message` без stack trace, поэтому точная строка падения не видна.
- Поскольку rescue job так и не стартует, `IsBeingRescued` всегда возвращает false, и система:
  - продолжает ставить новые Rescue‑задачи;
  - логирует тысячи ошибок;
  - фактически **никто не спасает лежащего колониста**, он умирает.

План исправления:

1. **Добавить stack trace**:
   - логировать `RimWatchLogger.Error("...", ex)` вместо только `ex.Message`.
2. **Усилить null‑проверки** в `ExecuteRescue` и `FindNearestAbleColonist`:
   - проверка `rescuer.jobs`, `health`, `capacities`, совпадение `Map`.
3. **Избежать бесконечной очереди Rescue**:
   - не ставить новую Rescue‑задачу для того же pawn, если уже в очереди/в работе.
4. **Троттлинг предупреждений**:
   - `No rescuer available for <pawn>` логировать через `WarningThrottledByKey`.

---

### 3. Медицинские emergency‑сигналы

Логи `MedicalAutomation`:

- множество повторов `🚨 MEDICAL EMERGENCY! 1 colonist(s) need immediate help!`
- регулярные сообщения:
  - `MedicalAutomation: 🚨 1 critically injured colonists!`
  - позже: `MedicalAutomation: 🚨 2 critically injured colonists!`

Что хорошо:

- Emergency‑детектор работает: корректно определяет downed / bleeding / critically injured.

Что плохо:

- Один и тот же emergency по одному и тому же pawn логируется десятки раз подряд.
- Нет чёткого trace: «нашли пациента → нашли доктора → поставили работу → работа стартовала/провалилась».

План улучшения:

1. **State‑based логирование**:
   - логировать emergency для конкретного пациента **только при изменении состояния**, либо с крупным cooldown (например, раз в 10–30 секунд).
2. **Structured logging**:
   - `LogDecision` / `LogExecutionStart/End` / `LogFailure` для цепочки:
     - обнаружен пациент;
     - повышены приоритеты Doctor;
     - найден доктор;
     - создан Tend/Rescue job;
     - успех/провал.

---

### 4. Строительство и отсутствие строителей

До критического момента:

- `ConstructionDiagnostics`:
  - `Found 26 unfinished frames!`
  - `2/3 colonists can construct`
  - число фреймов постепенно падает до 3 → колония строила, но медленно.

После 02:31:

- регулярные ошибки:
  - `❌ ConstructionDiagnostics: NO colonists can do Construction work!`
- `ConstructionMonitor` постоянно пишет:
  - `ConstructionMonitor: No colonists found on map`

Вывод:

- На финальном этапе **колонистов уже нет**, но диагностика продолжает писать одни и те же WARNING/ERROR по кругу.

План улучшения:

1. В `ConstructionDiagnostics` и `ConstructionMonitor`:
   - при `colonists.Count == 0`:
     - один раз логировать факт отсутствия колонистов;
     - дальше **тихо выходить** до появления колонистов.
2. Использовать `WarningThrottledByKey` для повторяющихся сообщений:
   - `construction_no_colonists`;
   - `construction_no_builders`.

---

### 5. DefenseAutomation – лог‑шум при постоянных врагах

Из лога:

- Тысячи сообщений вида:
  - `[DefenseAutomation] ⚠️ ENEMIES DETECTED: 4 hostiles on map!`
  - `DefenseAutomation: 🚨 RAID IN PROGRESS!`

Причина:

- `ManageDefense` логирует «враги на карте» **каждый вызов**, а при бою тик идёт быстро и долго.

План улучшения:

1. Ввести **состояние** (`LastEnemyCount`, `LastRaidState`):
   - логировать только при изменении количества врагов или статуса рейда.
2. Дополнительно можно:
   - логировать краткое summary по DefenseAutomation раз в N секунд;
   - а не каждый тик.

---

### 6. DecisionLogger (JSON) – только `building_placement` и проблемы с форматом

Из `decisions_2025-11-16.json`:

- `decision_type distribution: 670 x building_placement`.
- Примеры:
  - `building="Bed"` с большим `rejection_count` (~59) → показывает, что поиск места был непростой, но успешный.
  - `building="FueledStove"`:
    - `candidates: []`, `chosen: null` → **кухня так и не нашла место**.

Проблемы:

1. JSON формально невалиден:
   - файл начинается с `[` и на следующей строке просто `,` → это не корректный массив JSON.
2. Логируются только решения по строительству:
   - нет записей про work/farming/defense/medical decisions.

План улучшения:

1. Исправить формат:
   - либо proper JSON array (`[ obj1, obj2, ... ]` без лишних запятых),
   - либо `JSON Lines` (по объекту на строку без массива).
2. Расширить `decision_type`:
   - `work_prioritization`
   - `farming_management`
   - `defense_positioning`
   - `medical_triage`
   - и т.д.

---

### 7. Вывод: причины гибели колонии по логам

1. **Rescue‑цепочка была сломана**:
   - `ColonistCommandSystem` постоянно падал на `Rescue` с `NullReferenceException`.
   - Лежащих колонистов никто не мог отнести в кровати/медкровати.
2. **Медицина сигнализировала emergency, но не гарантировала завершение цепочки лечения**.
3. **Строительство и кровати**:
   - WorkAutomation поднимала приоритет Construction,
   - но часть фреймов и кухня так и оставались незавершёнными.
4. **В конце игры колонистов уже не было**, но:
   - DefenseAutomation, ConstructionMonitor и Diagnostics продолжали писать ошибки/предупреждения.

Таким образом:

- Логика автоматики (особенно Rescue и emergency) была недостаточно надёжной,
- Лог‑шум маскировал реальные проблемы,
- DecisionLogger показал явные провалы (кухня не построена).

---

### 8. План дальнейших изменений (high‑level)

1. **Реализовать structured logging API в `RimWatchLogger`**:
   - `LogCategory` enum;
   - `LogDecision`, `LogStateChange`, `LogExecutionStart/End`, `LogPerformance`, `LogFailure`;
   - `_failurePatterns` и `TrackFailurePattern`.
2. **Починить `ColonistCommandSystem` Rescue**:
   - полный stack trace в логах;
   - null‑safety в `ExecuteRescue` / `FindNearestAbleColonist`;
   - защита от бесконечной постановки Rescue‑задач.
3. **Задушить лог‑спам** в DefenseAutomation / MedicalAutomation / Construction* / WorkAutomation emergency:
   - state‑based logging;
   - `WarningThrottledByKey` для повторяющихся ситуаций.
4. **Расширить и починить DecisionLogger**:
   - валидный формат JSON;
   - поддержка других типов решений, не только строительства.

Эти изменения должны:

- сделать логи **читаемыми и осмысленными**;
- позволить быстро понимать, где автоматика не справляется;
- уменьшить шанс, что колония умирает «тихо», без понятного объяснения в логах.


