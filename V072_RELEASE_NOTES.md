# 📦 RimWatch v0.7.2 - Release Notes

**Дата релиза:** 2025-11-07  
**Версия:** 0.7.2  
**Совместимость:** RimWorld 1.6.4630+

---

## 🎯 Основные изменения

### ⚔️ Критическое исправление: Вооружение колонистов

**Проблема:**
При появлении врагов колонисты оставались безоружными, несмотря на наличие оружия на карте. Логи показывали:
```
⚠️ ENEMIES DETECTED: 2 hostiles on map!
⚠️ Only 0/3 colonists armed
```

**Причина:**
Система `AutoEquipWeapons` искала оружие только в `IsInAnyStorage()`, игнорируя:
- Оружие на земле
- Оружие в незавершенных хранилищах
- Оружие в неназначенных зонах

**Решение:**
1. **Расширен поиск оружия** - теперь ищется любое доступное оружие на карте
2. **Улучшена проверка** - создается список уже экипированного оружия и исключается из доступного
3. **Добавлено детальное логирование** всех этапов поиска и экипировки

**Код (до):**
```csharp
List<ThingWithComps> availableWeapons = map.listerThings.ThingsInGroup(ThingRequestGroup.Weapon)
    .OfType<ThingWithComps>()
    .Where(w => w.def.IsWeapon &&
               !w.IsForbidden(Faction.OfPlayer) &&
               w.Spawned &&
               w.IsInAnyStorage()) // ❌ Слишком строго
    .ToList();
```

**Код (после):**
```csharp
// Создаем список уже экипированного оружия
List<Pawn> allColonists = map.mapPawns.FreeColonistsSpawned.ToList();
List<Thing> equippedWeapons = allColonists
    .Where(p => p.equipment?.Primary != null)
    .Select(p => (Thing)p.equipment.Primary)
    .ToList();

// Ищем любое доступное оружие, кроме экипированного
List<ThingWithComps> availableWeapons = map.listerThings.ThingsInGroup(ThingRequestGroup.Weapon)
    .OfType<ThingWithComps>()
    .Where(w => w.def.IsWeapon &&
               !w.IsForbidden(Faction.OfPlayer) &&
               w.Spawned &&
               !equippedWeapons.Contains(w)) // ✅ Надежная проверка
    .OrderByDescending(w => w.GetStatValue(StatDefOf.RangedWeapon_DamageMultiplier))
    .ToList();
```

**Новые логи:**
```
DefenseAutomation: Found 3 unarmed colonists, searching for weapons...
DefenseAutomation: Found 5 available weapons: пистолет, дробовик, автомат, снайперка, лук
🗡️ DefenseAutomation: Equipped 3 colonists with weapons:
   • Джон → автомат (DMG: 1.5x, Shooting: 7)
   • Мария → снайперка (DMG: 1.8x, Shooting: 5)
   • Петр → дробовик (DMG: 1.2x, Shooting: 3)
```

---

### 🏗️ Критическое исправление: Размещение построек

**Проблема:**
Автопилот не мог разместить кухню и склад на раннем этапе игры. Логи показывали:
```
⚠️ Need a kitchen/stove!
BuildingAutomation: Could not find suitable location for kitchen

ℹ️ Need more storage space
BuildingAutomation: Could not find suitable location for storage zone
```

**Причина:**
Система `FindKitchenLocation` и `FindStorageLocation` требовали:
- `Roofed(map)` - наличие крыши
- `IsConstructedFloor(map)` - наличие построенного пола

На раннем этапе игры колония часто не имеет ни крыши, ни построенных полов!

**Решение:**
Смягчены требования для **радиуса 15 клеток от базы**:
- Крыша **предпочитается**, но не обязательна
- Построенный пол **предпочитается**, но не обязателен
- Достаточно просто `Standable(map)` + `CanPlaceBuildingAt()`

**Код (до - FindKitchenLocation):**
```csharp
if (candidate.Roofed(map) && 
    candidate.Standable(map) &&
    IsConstructedFloor(map, candidate) &&  // ❌ Слишком строго для старта
    CanPlaceBuildingAt(map, candidate, new IntVec2(2, 2)))
{
    candidates.Add(candidate);
}
```

**Код (после - FindKitchenLocation):**
```csharp
if (candidate.Standable(map) &&
    CanPlaceBuildingAt(map, candidate, new IntVec2(2, 2)))
{
    // Предпочитаем крышу, но принимаем и открытые места рядом с базой
    if (candidate.Roofed(map) || radius < 15)
    {
        candidates.Add(candidate);
    }
}
```

**Код (после - FindStorageLocation):**
```csharp
if (candidate.Standable(map))
{
    // Предпочитаем крышу, но принимаем и открытые места рядом с базой
    if (candidate.Roofed(map) || radius < 15)
    {
        return candidate;
    }
}
```

**Новые логи:**
```
🍳 BuildingAutomation: Placed kitchen stove blueprint at (127, 89)
📦 BuildingAutomation: Created storage zone at (125, 92) - 64 cells
```

---

## 📊 Сравнение: До vs После

| Аспект | v0.7.1 (до) | v0.7.2 (после) |
|--------|-------------|----------------|
| **Поиск оружия** | Только `IsInAnyStorage()` | Везде (земля + хранилище) |
| **Проверка экипировки** | Через `ParentHolder` (ненадежно) | Через список экипированного оружия |
| **Размещение кухни** | Требует крышу + пол | Работает на старте игры |
| **Размещение склада** | Требует крышу + пол | Работает на старте игры |
| **Логирование вооружения** | Отсутствует | Детальное на каждом шаге |
| **Логирование построек** | Минимальное | Информативное |

---

## 🔧 Технические детали

### Измененные файлы:

#### 1. `Source/RimWatch/Automation/DefenseAutomation.cs`
**Метод:** `AutoEquipWeapons` (строки 247-320)

**Изменения:**
- Убрана проверка `IsInAnyStorage()`
- Добавлен список экипированного оружия
- Добавлено 3 уровня логирования (поиск колонистов → поиск оружия → экипировка)
- Улучшена обработка случая "нет доступного оружия"

#### 2. `Source/RimWatch/Automation/BuildingAutomation.cs`
**Метод:** `FindKitchenLocation` (строки 400-452)

**Изменения:**
- Убрано требование `IsConstructedFloor()`
- Добавлена проверка `radius < 15` для смягчения требований
- Предпочтение крыше, но не обязательное требование

**Метод:** `FindStorageLocation` (строки 645-688)

**Изменения:**
- Убрано требование `IsConstructedFloor()`
- Добавлена проверка `radius < 15` для смягчения требований
- Предпочтение крыше, но не обязательное требование

---

## 🧪 Как протестировать?

### Тест 1: Вооружение при атаке
1. Запусти новую игру или загрузи сохранение
2. Дождись появления врагов (рейд, механоиды, дикие животные)
3. **Проверь логи:**
   - Должны появиться сообщения о поиске безоружных колонистов
   - Должны быть перечислены найденные оружия
   - Должны быть логи об экипировке конкретных колонистов
4. **Проверь игру:**
   - Колонисты должны подобрать оружие с земли или из хранилища
   - Приоритет - оружие с большим уроном

### Тест 2: Размещение кухни на старте
1. Запусти **новую игру** (важно - без построенных полов!)
2. Подожди ~2-3 минуты игрового времени
3. **Проверь логи:**
   - `⚠️ Need a kitchen/stove!`
   - `🍳 BuildingAutomation: Placed kitchen stove blueprint at (X, Z)`
4. **Проверь игру:**
   - Должен появиться blueprint печки рядом с базой

### Тест 3: Создание склада на старте
1. Запусти **новую игру** (важно - без построенных полов!)
2. Подожди ~2-3 минуты игрового времени
3. **Проверь логи:**
   - `ℹ️ Need more storage space`
   - `📦 BuildingAutomation: Created storage zone at (X, Z) - N cells`
4. **Проверь игру:**
   - Должна появиться зона склада (8x8) рядом с базой

---

## 🐛 Известные проблемы

### Не исправлено в этой версии:
1. **Отсутствие логов** - если вы не видите логов вообще, проверьте:
   - Включен ли автопилот (`RimWatch > Enable Autopilot`)
   - Включены ли конкретные категории автоматизации
   - Режим логирования в настройках RimWorld (`Options > Dev mode > Show dev log`)

2. **Колонисты не стреляют после вооружения** - это нормальное поведение RimWorld:
   - Колонисты должны быть задрафчены для боя
   - Автопилот драфтит колонистов только при близких врагах (<30 клеток)

3. **Неоптимальное размещение построек** - в текущей версии:
   - Постройки размещаются близко к базе, но не всегда идеально
   - Будущие версии улучшат логику выбора места

---

## 📝 Примечания разработчика

### Почему не используем `IsInAnyStorage()`?
В RimWorld 1.6 эта проверка может быть ненадежной:
- Не учитывает незавершенные хранилища
- Не учитывает зоны сброса (dumping stockpiles)
- Не учитывает оружие на полу в помещениях

Проверка через `equippedWeapons.Contains()` более надежна и явно показывает, что именно считается "недоступным".

### Почему смягчили требования для построек?
В классическом старте RimWorld:
1. Колония начинается на открытой местности
2. Первые дни нет крыши и построенных полов
3. Автопилот должен работать **с первого тика игры**

Требование крыши/пола **блокировало** автоматизацию на 1-2 игровых дня, что противоречит цели "полной автоматизации".

---

## 🚀 Установка

### Для пользователей:
1. Закрой RimWorld (если открыт)
2. Замени файлы мода новой версией
3. Запусти RimWorld
4. Загрузи сохранение или начни новую игру

### Для разработчиков:
```bash
cd /Users/ilyavolkov/Workspace/RimWorld-mods/RimWatch
make deploy
```

---

## 🎯 Что дальше?

### Запланировано для v0.7.3:
- Улучшение логики размещения построек (учет соседних комнат, электрической сети)
- Автоматическая установка дверей между помещениями
- Оптимизация выбора культур для growing zones

### Запланировано для v0.8:
- Умные стратегии для каждой категории автоматизации
- Адаптация к разным биомам и климату
- Продвинутое управление караванами

---

## 📖 Дополнительные материалы

- **[Детальное описание исправлений](BUGFIX_V072.md)**
- **[Краткая сводка](V072_SUMMARY.md)**
- **[Руководство по тестированию](TESTING_GUIDE.md)**
- **[README](README.md)**
- **[ROADMAP](ROADMAP.md)**

---

## 🙏 Благодарности

Спасибо **пользователям-тестерам** за детальные логи и отчеты об ошибках!  
Ваша помощь критически важна для улучшения мода.

---

**Статус:** ✅ Скомпилировано и развернуто (2025-11-07)  
**Версия:** 0.7.2  
**Совместимость:** RimWorld 1.6.4630+

